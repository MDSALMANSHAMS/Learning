#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    // Calculating the square root of a number
    double number = 25.0;
    double squareRoot = sqrt(number);
    cout << "Square root of " << number << " is: " << squareRoot << endl;

    // // Calculating the value of sine function
    // double angle = 45.0;                          // Angle in degrees
    // double sineValue = sin(angle * M_PI / 180.0); // Convert angle to radians before using sin function
    // cout << "Sine of " << angle << " degrees is: " << sineValue << endl;

    return 0;
}
